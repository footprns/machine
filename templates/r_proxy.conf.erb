# create new
<IfModule mod_proxy.c>
    ProxyRequests Off
    <Proxy *>
        Require all granted
    </Proxy>
    # backend server and forwarded path
    # ProxyPass / http://40.68.218.73/
    # ProxyPassReverse / http://40.68.218.73/
    ProxyPass / http://<%= @target_ip %>/
    ProxyPassReverse / http://<%= @target_ip %>/
</IfModule>
